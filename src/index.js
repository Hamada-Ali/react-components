import './index.css'
import React from "react";
import  ReactDOM  from 'react-dom/client'
import App from './App.js'

const root = document.getElementById('root')

const controlRoot = ReactDOM.createRoot(root)

controlRoot.render(<App />)